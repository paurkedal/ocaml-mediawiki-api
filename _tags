true: bin_annot

"lib": include

true: \
    package(calendar), \
    package(cohttp), package(cohttp.lwt), \
    package(conduit), package(conduit.lwt), package(conduit.lwt-unix), \
    package(kojson), \
    package(lwt), \
    package(ppx_compose), \
    package(prime), \
    package(ssl), \
    package(xdg-basedir), \
    package(yojson)

<lib/mwapi_lwt.*>: package(lwt.ppx)

<bin/*>: \
    package(cmdliner), \
    package(lwt.ppx), \
    package(pcre)
