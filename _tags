true: bin_annot

"lib": include

true: \
    package(calendar), \
    package(cohttp), package(cohttp.lwt), \
    package(kojson), \
    package(lwt), \
    package(ppx_compose), \
    package(prime), \
    package(ssl), \
    package(xdg-basedir), \
    package(yojson)

<bin/*>: \
    package(cmdliner), \
    package(pcre)
